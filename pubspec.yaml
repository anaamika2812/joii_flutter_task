name: joii_flutter_task
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.6.0
  flutter_bloc: ^8.1.6
  get: ^4.6.6
  shared_preferences: ^2.3.2
  fluttertoast: ^8.2.8
  fl_chart: ^0.68.0  # For animated chart on dashboard
  google_fonts: ^6.2.1  # For nice fonts
  mockito: ^5.0.0
  build_runner: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter


flutter:
  uses-material-design: true
